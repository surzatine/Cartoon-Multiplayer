using UnityEngine;

public class HostGame : MonoBehaviour
{
    public void StartHost()
    {
        //FishNet.Managing.NetworkManager nm =
        //    FishNet.Managing.NetworkManager.Singleton;

        //if (nm == null)
        //{
        //    Debug.LogError("FishNet NetworkManager not found in scene!");
        //    return;
        //}

        //nm.ServerManager.StartConnection();
        //nm.ClientManager.StartConnection();

        //Debug.Log("Host started successfully");
    }
}
